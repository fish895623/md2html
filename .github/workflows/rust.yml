name: Rust

on: "push"
env:
  CARGO_TERM_COLOR: always
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: cargo build
      - name: setup tarpaulin
        run: cargo install cargo-tarpaulin
      - name: Run tests
        run: cargo tarpaulin --queit -- --nocapture --color=always --shuffle -Z unstable-options
